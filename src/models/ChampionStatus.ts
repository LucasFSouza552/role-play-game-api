import { statValue } from "./StatsValue";

export interface ChampionStatus {
    strength: number;
    dexterity: number;
    intelligence: number;
    vitality: number;
    XP: statValue;
    Level: number;
}